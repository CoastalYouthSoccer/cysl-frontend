name: Build and Deploy Frontend Code

on:
    push:
      paths:
        - '**'
        - '!README.md'
        - '!VERSION'
      branches:
        - '**'
        - '!releases/**'

jobs:
  build_deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
    - run: |
        npm install
        npm run test
        npm run build

    - name: Deploy to Server
      uses: easingthemes/ssh-deploy@main
      with:
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        ARGS: "-rlgoDzvc -i"
        SOURCE: "dist/"
        REMOTE_HOST: ${{ vars.DEPLOY_HOST }}
        REMOTE_USER: ${{ vars.DEPLOY_USER }}
        TARGET: ${{ vars.DEPLOY_TARGET }}/frontend-files/${{ github.sha }}
        SCRIPT_BEFORE: |
          sudo mkdir -p ${{ vars.DEPLOY_TARGET }}/frontend-files/${{ github.sha }}
          sudo chmod 777 -R ${{ vars.DEPLOY_TARGET }}/frontend-files/${{ github.sha }}
        SCRIPT_AFTER: |
          sudo rm ${{ vars.DEPLOY_TARGET }}/frontend || echo frontend link does NOT exist
          sudo ln -s ${{ vars.DEPLOY_TARGET }}/frontend-files/${{ github.sha }} ${{ vars.DEPLOY_TARGET }}/frontend
          sudo chmod 744 -R ${{ vars.DEPLOY_TARGET }}/frontend-files/${{ github.sha }}
          sudo chown root:root -R ${{ vars.DEPLOY_TARGET }}/frontend-files/${{ github.sha }}
